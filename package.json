{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "tsc --build packages.tsconfig.json",
    "watch": "tsc --build --watch packages.tsconfig.json"
  },
  "devDependencies": {
    "lerna": "^3.14.1"
  }
}
